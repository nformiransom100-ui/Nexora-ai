const express=require('express');const path=require('path');const app=express();app.use(express.json());app.use(express.static(__dirname));app.post('/api/chat',async(req,res)=>{const key=process.env.OPENROUTER_API_KEY;if(!key)return res.status(500).json({error:'OpenRouter key missing'});const{messages=[],mode='chat'}=req.body;const prompts={chat:'You are Nova AI, a helpful assistant.',code:'You are Nova AI, an expert coding assistant.',write:'You are Nova AI, a writing assistant.',analyze:'You are Nova AI, a data analyst.',brainstorm:'You are Nova AI, a creative brainstorming partner.',advice:'You are Nova AI, a helpful advisor.',summarize:'You are Nova AI, a summarization expert.',translate:'You are Nova AI, a translation assistant.'};try{const r=await fetch('https://openrouter.ai/api/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json','HTTP-Referer':'https://nova-ai.app','X-Title':'Nova AI'},body:JSON.stringify({model:'anthropic/claude-3-haiku',messages:[{role:'system',content:prompts[mode]||prompts.chat},...messages],max_tokens:2048})});const data=await r.json();res.json({content:data.choices?.[0]?.message?.content||''});}catch(err){res.status(500).json({error:err.message})}});app.get('*',(req,res)=>res.sendFile(path.join(__dirname,'index.html')));app.listen(process.env.PORT||3000,'0.0.0.0',()=>console.log('Nova AI running'));
