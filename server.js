const express=require('express');const path=require('path');const app=express();app.use(express.json());app.use(express.static(__dirname));app.post('/api/chat',async(req,res)=>{const key=process.env.OPENROUTER_API_KEY;if(!key)return res.status(500).json({error:'OpenRouter key missing'});const{messages=[],mode='chat'}=req.body;const prompts={chat:'You are Nova AI, a helpful assistant.',code:'You are Nova AI, a coding assistant. Use code blocks.',write:'You are Nova AI, a writing assistant.',analyze:'You are Nova AI, a data analyst.',brainstorm:'You are Nova AI, a creative partner.',advice:'You are Nova AI, a helpful advisor.',summarize:'You are Nova AI, summarize clearly.',translate:'You are Nova AI, translate accurately.'};try{const r=await fetch('https://openrouter.ai/api/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json','HTTP-Referer':'https://nova-ai.app','X-Title':'Nova AI'},body:JSON.stringify({model:'meta-llama/llama-3.1-8b-instruct:free',messages:[{role:'system',content:prompts[mode]||prompts.chat},...messages],max_tokens:1024})});const data=await r.json();const content=data.choices?.[0]?.message?.content||'';res.json({content});}catch(err){res.status(500).json({error:err.message})}});app.get('*',(req,res)=>res.sendFile(path.join(__dirname,'index.html')));app.listen(process.env.PORT||3000,'0.0.0.0',()=>console.log('Nova AI running'));
